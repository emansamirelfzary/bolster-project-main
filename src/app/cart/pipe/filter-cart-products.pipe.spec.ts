import { FilterCartProductsPipe } from './filter-cart-products.pipe';

describe('FilterCartProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCartProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
